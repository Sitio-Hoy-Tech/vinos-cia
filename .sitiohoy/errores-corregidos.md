# Errores corregidos durante el desarrollo

> Registrar acá problemas reales encontrados al implementar páginas o módulos.
> Este archivo sirve para mejorar las skills de SitioHoy después de cada proyecto.

| Fecha (-03:00) | Página/Módulo | Error detectado | Causa | Solución aplicada | Archivo(s) tocados | Recomendación para skills |
|---|---|---|---|---|---|---|
| 2026-05-23 | Módulo 0 — Scaffold | `Cannot find module '@/lib/config/tenant'` en build | Template usa `@/lib/config/tenant` pero la función está en `@/lib/supabase/tenant` | Corregir import en `lib/resend/client.ts` | `lib/resend/client.ts` | Actualizar template para que el import apunte a `@/lib/supabase/tenant` |
| 2026-05-23 | Módulo 3 — Home (500 error) | Home devuelve 500 al iniciar el dev server | `next/image` rechaza URLs de Unsplash porque `images.unsplash.com` no está en `remotePatterns` de `next.config.ts` | Agregar `{ protocol: 'https', hostname: 'images.unsplash.com' }` a `remotePatterns` | `next.config.ts` | El scaffold/template debe incluir `images.unsplash.com` en `remotePatterns` por defecto, ya que los seeds usan Unsplash como placeholder |
| 2026-05-23 | Módulo 3-4 — Catálogo (cache en dev) | Cambios del admin no se reflejan en `next dev` porque `unstable_cache` cachea incluso en desarrollo | `unstable_cache` no distingue entre dev y prod; el webhook de Supabase pg_net no puede llamar a `localhost` | Saltear `unstable_cache` cuando `NODE_ENV === 'development'` con helper `withCache`/`withCacheArgs` | `lib/data/products.ts`, `lib/data/categories.ts` | La skill debe incluir este patrón de dev/prod en todos los archivos `lib/data/*.ts` desde el scaffold |
| 2026-05-23 | Módulo 3-4 — Productos (oferta falsa) | Productos del seed muestran badge de descuento cuando no están en oferta | El seed insertó `compare_at_price` con valores mayores al `price` en todos los productos demo | Limpiar `compare_at_price` con `UPDATE products SET compare_at_price = null` en la BD | Seed SQL / BD Supabase | El seed demo no debe incluir `compare_at_price` salvo en productos explícitamente marcados como oferta |
| 2026-05-23 | Módulo 3-4 — ISR en producción | El ISR on-demand no dispara porque `tenants.url` queda `null` después del seed | El seed no completa el campo `url` del tenant con la URL de producción (que no se conoce hasta el deploy) | Al hacer deploy a Vercel, correr `UPDATE tenants SET url = 'https://...' WHERE id = '...'` | `supabase/migrations/002_seed_admin.sql` | Documentar explícitamente en el módulo de deploy que hay que actualizar `tenants.url`; o pedirlo en el briefing como "URL temporal de Vercel" |
| 2026-05-23 | Módulo 6 — Blog | `column blog_posts.author_name does not exist` en build | La migración SQL incluye la columna pero la tabla real en Supabase no la tiene (schema drift) | Quitar `author_name` del SELECT en `lib/data/blog.ts` | `lib/data/blog.ts`, `app/blog/[slug]/page.tsx` | Ejecutar `supabase gen types` antes de escribir queries para detectar columnas inexistentes; o agregar columna con `ALTER TABLE` si hace falta |
| 2026-05-23 | Módulo 6 — Contacto (email config) | Configuración de email hardcodeada en `.env` (`RESEND_API_KEY`, `RESEND_FROM_EMAIL`, `RESEND_FROM_NAME`) en lugar de venir del tenant | El scaffold generaba variables de entorno para datos que el admin debe poder cambiar desde el panel | Leer toda la config de email desde `tenants`: `resend_api_key` → clave Resend (sin fallback a `.env`, ni en dev ni en prod), `contact_email` → remitente (`from`) y destinatario de notificaciones internas, `name` → nombre del remitente. Si `resend_api_key` está vacío en el tenant, los emails se saltan pero el mensaje queda guardado en `contact_messages` | `lib/actions/contact.ts` | El scaffold NO debe generar `RESEND_API_KEY`, `RESEND_FROM_EMAIL` ni `RESEND_FROM_NAME` en `.env`; los tres viven en `tenants`. `RESEND_API_KEY` en `.env` queda eliminado — siempre leer `tenants.resend_api_key`, incluso en local |
| 2026-05-23 | Todos los módulos — WhatsApp | Número de WhatsApp hardcodeado en múltiples componentes (`hero.tsx`, `footer.tsx`, `whatsapp-button.tsx`, `faq-inline.tsx`) | El template/scaffold genera componentes con número fijo en lugar de leerlo desde el tenant | Agregar columna `whatsapp text` a `tenants`, leerla en `getTenantConfig()`, pasar como prop desde `layout.tsx` y `page.tsx` | `supabase/migrations/003_tenant_whatsapp.sql`, `lib/supabase/tenant.ts`, `app/layout.tsx`, `app/page.tsx`, todos los componentes UI | La migración base debe incluir `whatsapp` en `tenants` desde el inicio; el scaffold debe generar los componentes sin números hardcodeados, siempre recibiéndolo como prop |
| 2026-05-23 | Módulo 3 — Categorías | `CategoriesGrid` usaba mapa de imágenes hardcodeado por slug (`CATEGORY_CONFIG`) | El template asume que las categorías tienen imágenes fijas, ignorando las imágenes reales de los productos cargados | Reemplazar con `getCategoriesWithImages()` que: (1) filtra categorías sin productos, (2) obtiene la imagen del último producto subido por categoría, (3) usa Unsplash como fallback por slug | `lib/data/categories.ts`, `components/home/categories-grid.tsx` | La skill debe generar `getCategoriesWithImages()` desde el inicio; las categorías sin productos no deben mostrarse en el grid; la imagen debe derivarse del catálogo real, no de un config estático |
| 2026-05-23 | Módulo 5 — MercadoPago rechaza localhost en back_urls y notification_url | Error `auto_return invalid. back_url.success must be defined` (status 400) en desarrollo local | MP rechaza `back_urls`, `auto_return` y `notification_url` con `http://localhost` — sus servidores no pueden alcanzar localhost y considera las URLs inválidas | Omitir los tres campos cuando `NEXT_PUBLIC_URL.startsWith('http://localhost')`. En producción se envían normalmente | `lib/actions/checkout.ts` | El scaffold debe incluir este condicional desde el inicio. En local, para probar el flujo completo usar ngrok y actualizar `NEXT_PUBLIC_URL` temporalmente |
| 2026-05-23 | Módulo 4 — ProductCard con variantes ignoraba stock | Productos con variantes podían agregarse al carrito desde la card con `stockUnlimited: true`, saltando los límites de stock de las variantes | `product-card.tsx` pasaba `stockUnlimited: product.stock_unlimited` directamente sin considerar que las variantes tienen su propio stock limitado | Agregar `product_variants(id)` a las queries de catálogo y home. En `ProductCard`, si `hasVariants`, mostrar "Ver opciones" (Link al detalle) en lugar de `AddToCartButton`. El stock de variantes solo se gestiona en la página de detalle | `lib/data/products.ts`, `components/ui/product-card.tsx` | El scaffold debe incluir `product_variants(id)` en todas las queries de listado de productos y `ProductCard` debe siempre mostrar "Ver opciones" para productos con variantes |
| 2026-05-23 | Módulo 4 — Detalle de producto (cantidad ignoraba carrito) | Hacer click varias veces en "Agregar" sumaba unidades al carrito superando el stock disponible | El selector de cantidad solo limitaba contra el stock total, sin considerar cuántas unidades ya estaban en el carrito | Leer el carrito con `useCartStore` en `product-client.tsx`, calcular `maxAddable = stock - cartQuantity`, usarlo como tope del selector y del botón. Mostrar aviso "Ya tenés X en el carrito" cuando corresponde | `components/product/product-client.tsx` | El selector de cantidad en la página de producto siempre debe descontar lo que ya está en el carrito para calcular el máximo agregable |
| 2026-05-23 | Módulo 4 — Detalle de producto (oferta con variante) | Precio de oferta no se mostraba cuando el producto tenía variantes | `hasDiscount` usaba `!selectedVariant &&` — si había variante, nunca se evaluaba el descuento. Además `discountPct` calculaba contra `product.price` en vez de `effectivePrice` | Quitar `!selectedVariant &&` de la condición y calcular el descuento siempre contra `effectivePrice` (precio real de la variante seleccionada) | `components/product/product-client.tsx` | El `compare_at_price` es un campo del producto, no de la variante — el descuento debe mostrarse comparando contra el precio efectivo (variante o producto) en todo momento |
| 2026-05-23 | Módulo 6 — Emails (sin diseño) | Los emails enviados con Resend llegaban sin diseño (texto plano) | Los templates usaban `text:` en vez de `html:` en `resend.emails.send()` | Crear `lib/email/templates.ts` con funciones que devuelven HTML completo con estilos inline. Tres templates: `contactConfirmationEmail`, `contactNotificationEmail`, `orderConfirmationEmail`. Usar siempre `html:` en Resend. Los estilos DEBEN ser inline — los clientes de email ignoran `<style>` y CSS externo | `lib/email/templates.ts`, `lib/actions/contact.ts`, `app/api/webhook/mercadopago/route.ts` | El scaffold debe generar `lib/email/templates.ts` desde el inicio con templates HTML inline. Nunca enviar emails con solo `text:` sin `html:` — siempre incluir ambos o solo `html:` |
| 2026-05-23 | Módulo 4 — Catálogo | Productos sin stock no mostraban el badge "Sin stock" en el grid de catálogo | El seed demo tenía `stock_unlimited = true` en todos los productos, por lo que la condición `outOfStock` nunca era verdadera | Ejecutar `UPDATE products SET stock_unlimited = false WHERE stock = 0` en la BD para que el flag refleje la realidad | BD Supabase (query manual) | El seed demo debe insertar productos con `stock_unlimited = false` y distintos valores de `stock` (incluyendo 0) para poder hacer QA del flujo de sin stock desde el primer día |
| 2026-05-23 | Módulo 5 — Checkout (Wallet Brick) | El checkout usaba el `Wallet` brick de MercadoPago, que es un botón que redirige al sitio de MP, no un formulario de pago embebido | `@mercadopago/sdk-react` ofrece dos bricks: `Wallet` (redirect) y `Payment` (formulario embebido). La skill generó `Wallet` por defecto | Reemplazar `<Wallet>` con `<Payment>`. El `Payment` brick recibe `amount`, `preferenceId`, `payer.email` en `initialization` y llama a `onSubmit` con `{ selectedPaymentMethod, formData }` — el `formData` contiene el `token` de la tarjeta que debe procesarse server-side con `MercadoPagoPayment.create()`. Agregar `processPayment` Server Action que recibe el token y crea el pago en MP. Redirigir a `/pedidos/[token]` si aprobado o a `/checkout/pending` si pendiente | `app/checkout/_components/checkout-form.tsx`, `lib/actions/checkout.ts` | La skill debe generar directamente el `Payment` brick, no el `Wallet`. El `Wallet` es solo válido cuando querés redirigir al usuario fuera del sitio (flujo externo) |
| 2026-05-24 | Deploy — README faltante | El repositorio se subió a GitHub con el README por defecto de `create-next-app` en lugar de uno con la documentación real del proyecto | El scaffold no reemplaza el README generado por Next.js. Al momento del deploy el repo no tenía stack, arquitectura, variables de entorno ni checklist documentados | Antes de hacer deploy y push al repo, siempre generar el `README.md` con: stack, arquitectura de carpetas, variables de entorno, comandos de desarrollo, integraciones y checklist pre-go-live | `README.md` | El módulo de deploy debe incluir la generación del README como paso obligatorio previo al `git push` y al `vercel --prod`. Un repo sin README adecuado no debe considerarse listo para entregar |
| 2026-05-24 | Deploy — Repositorios GitHub | Repositorio creado en cuenta personal (`valentinsitiohoy`) en lugar de la organización | El comando `gh repo create` por defecto usa la cuenta autenticada, no la organización del proyecto | Siempre crear repositorios en la organización `Sitio-Hoy-Tech` usando `gh repo create Sitio-Hoy-Tech/nombre-repo --private` | `gh repo create` | El scaffold y el módulo de deploy deben crear el repo directamente en `Sitio-Hoy-Tech`. Nunca usar `gh repo create nombre-repo` sin prefijo de organización |
| 2026-05-24 | Módulo 0 — ISR tenant (credenciales sin efecto) | Cambiar credenciales de MP (u otros datos del tenant) en Supabase no impactaba en producción hasta hacer un revalidate manual | El trigger ISR de la migración inicial no incluía la tabla `tenants`. Solo existían triggers para `products`, `categories`, `blog_posts`, etc. | Crear migración `004_isr_tenants_trigger.sql` con `trigger_isr_tenants()` que llama a `isr_notify(NEW.id, 'tenants')` en INSERT/UPDATE/DELETE | `supabase/migrations/004_isr_tenants_trigger.sql` | La migración base siempre debe incluir el trigger ISR para `tenants`. Cualquier cambio de configuración del negocio (MP, Resend, WhatsApp, nombre) debe invalidar el cache del tenant automáticamente |
| 2026-05-24 | Módulo 5 — ISR checkout (precio desactualizado) | El checkout mostraba precios viejos del carrito aunque el precio del producto hubiera cambiado en el admin | `CheckoutForm` lee precios desde Zustand/localStorage sin verificar contra la BD al montar. El usuario podía completar el pago con precios desactualizados | Llamar a `refreshCartPrices` en un `useEffect` al montar `CheckoutForm`, igual que en el drawer del carrito. Los precios se actualizan antes de que el usuario pueda enviar el formulario | `app/checkout/_components/checkout-form.tsx` | Cualquier componente que muestre o calcule precios del carrito debe refrescarlos desde la BD al montar. Patrón universal: `refreshCartPrices` en `useEffect([], [])` en cada punto de entrada donde se muestren precios (drawer, checkout, resumen de orden) |
| 2026-05-24 | Módulo 3-4 — ISR carrito (precio desactualizado) | El precio en el carrito no se actualizaba al cambiar el precio del producto en el admin | El carrito persiste precios en `localStorage` (Zustand persist). El ISR invalida el cache del servidor pero nunca actualiza el estado cliente que ya fue hidratado | Agregar Server Action `refreshCartPrices` que consulta precios actuales. Llamarla cada vez que se abre el drawer (`isOpen` cambia a `true`) y actualizar el store con `updatePrices`. Así el usuario siempre ve precios vigentes al momento de revisar su carrito | `lib/actions/cart.ts`, `lib/store/cart.ts`, `components/cart/cart-drawer.tsx` | El ISR solo garantiza frescura en Server Components — cualquier dato persistido en `localStorage` (carrito, wishlist, preferencias) queda desactualizado y debe re-fetchearse desde el servidor al mostrarse. Patrón: refrescar datos cliente al abrir el componente que los muestra |
| 2026-05-24 | Módulo 3-4 — ISR detalle de producto | Cambio de precio impactaba en el home/catálogo pero no en `/productos/[slug]` | `getProductBySlug` solo tenía el tag `TAGS.PRODUCT(slug)` — el trigger de Supabase manda `table: products` sin slug, por lo que solo se invalidaba `TAGS.PRODUCTS` (lista) y nunca el cache del detalle | Agregar `TAGS.PRODUCTS` como segundo tag en `getProductBySlug`: `{ tags: [TAGS.PRODUCT('{slug}'), TAGS.PRODUCTS] }` | `lib/data/products.ts` | Toda query de detalle que dependa de datos editables debe incluir el tag de lista como segundo tag, para que cualquier invalidación masiva del recurso también limpie el detalle |
| 2026-05-24 | Deploy — Vercel (repo privado) | El deploy en Vercel quedaba con status `UNKNOWN` y nunca completaba el build | Repositorio privado en una organización de GitHub con cuenta Vercel Hobby/sin plan Pro de equipo — Vercel no puede acceder al repo privado y el build queda colgado indefinidamente | Hacer el repositorio público en GitHub con `gh repo edit Sitio-Hoy-Tech/nombre-repo --visibility public --accept-visibility-change-consequences` antes de deployar | `gh repo edit` | En proyectos con cuenta Vercel Hobby o team sin plan Pro, crear el repositorio directamente como público. Si el cliente requiere privacidad, evaluar Vercel Pro antes del deploy |
| 2026-05-24 | Módulo 5 — Webhook MercadoPago (estado no actualiza) | El webhook devolvía 200 OK pero el pedido quedaba en `PENDIENTE PAGO` sin actualizarse | El handler verificaba `payload.type === 'payment'` (formato legacy MP v1), pero MP actualmente envía formato v2 con `{ action: 'payment.created', data: { id } }` — sin campo `type`. La condición fallaba y retornaba 200 sin procesar | Agregar soporte dual: `isPaymentEvent = payload.type === 'payment' \|\| payload.action?.startsWith('payment')`. Usar variable `paymentId = payload.data?.id` para el get | `app/api/webhook/mercadopago/route.ts` | El handler siempre debe aceptar ambos formatos. Formato legacy: `{ type: 'payment', data: { id } }`. Formato v2: `{ action: 'payment.created', data: { id } }`. Nunca asumir solo uno |
